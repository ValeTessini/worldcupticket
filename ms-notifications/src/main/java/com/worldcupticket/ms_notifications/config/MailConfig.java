package com.worldcupticket.ms_notifications.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MailConfig {
    // Mail configuration is accomplished via application.properties.
}
